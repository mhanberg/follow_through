<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <%= if logged_in?(@conn), do: tag :meta,
          name: "channel_token",
          content: Phoenix.Token.sign(@conn, "user salt", current_user(@conn).id) %>
    <title>FollowThrough</title>
    <link rel="stylesheet" href="https://rsms.me/inter/inter-ui.css"/>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body class="font-sans text-grey-800">
    <main role="main" class="border-t-7 border-blue-800 bg-grey-200 min-h-screen">
      <header class="container py-4 mb-4">
        <div class="flex items-end justify-between mx-2">
          <%= link to: "/" do %>
            <h2 class="font-medium text-2xl">Follow Through</h2>
          <% end %>
          <div>
            <%= unless logged_in?(@conn) do %>
              <%= link to: "/auth/github", class: "btn-primary flex items-center normal-case" do %>
                <%= svg_image(@conn, "octocat", class: "h-6 w-6 mr-4") %>
                Sign in with GitHub
              <% end %>
            <% else %>
              <div class="flex">
                <%= link "New Team", to: Routes.team_path(@conn, :new), class: "text-lg mr-4" %> 
                <%= link "Logout", to: "/auth/logout", method: :delete, class: "text-lg" %>
              </div>
            <% end %>
          </div>
        </div>
      </header>
      <%= render "_flash.html", conn: @conn %>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
